<%= form_for(@comment) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :text, placeholder: "コメントして連絡を取ろう！" %>
  </div>
  <%= f.submit "コメントを送信する", class: "btn btn-large btn-primary" %>
<% end %>